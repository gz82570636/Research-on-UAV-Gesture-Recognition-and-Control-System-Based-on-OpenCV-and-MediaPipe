#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
================================================================================
香橙派端手势控制系统 - README
================================================================================

📁 文件结构:
    config.py              - 配置参数（摄像头、网络、手势映射）
    drone_controller.py    - 无人机控制器（ROS连接、位置控制）
    gesture_recognizer.py  - 手势识别模块
    landmark_smoother.py   - 关键点平滑滤波
    main.py                - 主程序入口
    start_orangepi.sh      - 启动脚本

🎯 功能说明:
    本系统运行在香橙派3B上，通过摄像头识别手势，
    然后通过ROS网络控制虚拟机中Gazebo的无人机飞行。

    核心设计：手势 = 虚拟键盘
        - 食指向上   → 按W键 → 无人机向前飞
        - 食指向下   → 按S键 → 无人机向后飞
        - 食指向左   → 按A键 → 无人机向左飞
        - 食指向右   → 按D键 → 无人机向右飞
        - 左手张开   → 按1键 → 解锁+悬停2m
        - 左手握拳   → 按3键 → 降落

🌐 网络配置:
    香橙派IP:      192.168.1.12
    虚拟机IP:      192.168.1.11
    ROS Master:    http://192.168.1.11:11311

🚀 使用方法:

    1. 在虚拟机上启动仿真（先做这个）:
       cd ~/work_vm/vm_control
       ./start_px4_sitl.sh

    2. 在香橙派上启动手势控制:
       cd ~/work/orangepi_control
       ./start_orangepi.sh

       或者手动运行:
       export ROS_MASTER_URI=http://192.168.1.11:11311
       export ROS_IP=192.168.1.12
       python3 main.py --vm-ip 192.168.1.11

📋 手势说明:

    右手食指指向（食指伸直，其他手指弯曲）:
        - 向上:    食指指向摄像头上方 → 无人机前进
        - 向下:    食指指向摄像头下方 → 无人机后退
        - 向左:    食指指向摄像头左侧 → 无人机左移
        - 向右:    食指指向摄像头右侧 → 无人机右移
        - 斜向:    食指指向斜角 → 无人机斜向移动

    左手手势（看手指状态）:
        - 张开:    五指都伸直 → 解锁+悬停（如果未解锁）
        - 握拳:    五指都弯曲 → 降落（如果已在飞行）

    保持时间:
        - 所有手势都需要保持至少0.4秒才会生效
        - 持续指向时，每0.3秒移动一步0.5米

🔧 安装依赖:
    pip3 install mediapipe opencv-python numpy
    # ROS相关库通常随ROS安装，不需要额外安装

⚠️  注意事项:
    1. 确保虚拟机和香橙派在同一网络（桥接模式）
    2. 确保虚拟机先启动仿真环境
    3. 检查摄像头设备路径（默认/dev/video3）
    4. 如果方向反了，检查config.py中的GESTURE_STEP映射

🐛 故障排查:
    - 无法连接到ROS Master: 检查网络、ping 192.168.1.11
    - 摄像头打不开: ls /dev/video* 查看可用设备
    - 手势不识别: 确保光线充足，手部在摄像头视野内
    - 无人机不响应: 检查虚拟机Gazebo中是否有无人机模型

📧 技术细节:
    - 控制方式: 位置控制（PositionTarget）
    - 发布话题: /mavros/setpoint_raw/local
    - 帧率: 15fps
    - 分辨率: 640x480
    - 手势保持阈值: 0.4秒
    - 位置步长: 0.5米
================================================================================
"""

# 这个文件是README说明，不需要运行
print("请阅读README.md或查看文件中的注释了解使用方法")